# To install Google Translator module, use `pip install googletrans==4.0.0-rc1` in the Terminal window
import googletrans

# List all available languages in the module and their abbreviations
print(googletrans.LANGUAGES)